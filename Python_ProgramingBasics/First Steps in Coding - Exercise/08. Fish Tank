# Write a program that calculates the liters of water needed to fill the aquarium.

length = int(input())
wide = int(input())
height = int(input())
occupied_volume = float(input())

volume_l = (length * wide * height) * 0.001
occupied_volume = occupied_volume * 0.01
print(volume_l*(1-occupied_volume))
