#Calculate necessary hours an architect will need to draw projects of several construction sites.

architect_name = input()
projects_number = int(input())
h_hours = projects_number * 3
print(f'The architect {architect_name} will need {h_hours} hours to complete {projects_number} project/s.')
