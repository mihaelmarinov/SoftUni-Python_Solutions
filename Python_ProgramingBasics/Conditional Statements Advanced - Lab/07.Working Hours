# Write a program that reads an hour of the day (integer) and a day of the week (text) - entered by the user and checks if the company's office is open.

hours = int(input())
day_of_week = str(input())

work_days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]

if 10 <= hours <= 18 and day_of_week in work_days:
    print("open")
else:
    print("closed")
