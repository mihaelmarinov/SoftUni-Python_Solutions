# The first task of this topic is to write a console program that reads an estimate (real number), entered
# by the user and prints "Excellent!" if the rating is 5.50 or higher.

grade = float(input())

if grade >= 5.50:
    print("Excellent!")
